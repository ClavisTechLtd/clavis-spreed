---
topic: Clavis Talk Thread Management and Navigation
updated: 2026-08-08T21:25
---

- (event) PRD run bound; intent=Create; source=GitHub issue ClavisTechLtd/clavis-deploy#22 (Clavis Talk thread management)
- (decision) Scope = backend + web in clavis-spreed + notification payload; mobile clients (Android/iOS) deferred to follow-up epic because clavis-push APNs/FCM delivery hop is unimplemented and blocked on credentials
- (decision) Federation out of scope: upstream itself has FIXME 'support threads in federation' at ChatController.php:898 and :1273; we do not take on that debt
- (decision) Unread per thread computed from existing talk_attendees.last_read_message vs talk_threads.last_message_id; no per-thread read-marker rows, because upstream created then deliberately dropped those columns (Version22000Date20250623142327 -> Version22000Date20250710124258)
- (decision) Thread states: Ongoing / Closed / Locked. Closed is soft (label + hidden from default list, posting revives to Ongoing). Locked is hard (blocks all writes, only thread author or room moderator can reopen)
- (decision) Labels are free-text tags with colour; colour is bound to the tag string at room scope so the same tag text renders one colour everywhere in that room
- (assumption) Permission model for state/pin/label mirrors existing renameThread rule (ThreadController.php:224-228): thread root-message author OR room moderator
- (event) Root cause found for issue item 8: Notifier.php:633 guards the threadId setObject() behind !isPreparingPushNotification(), so push payloads never carry threadId. Server-side defect, not a client gap
- (event) Defect found: Notifier.php:637-641 overwrites setObject(token/messageId) with setObject(token/threadId), losing messageId - client can reach the thread but not the specific message
- (event) Unread model confirmed viable: talk_attendees.last_read_message exists (Version7000Date20190724121136:41); the dropped columns were on talk_thread_attendees, a different table. ChatManager::getUnreadCount(Room,int):993 is the pattern to extend
- (event) Push subject budget is tight: Notifier.php:686-689 truncates the message preview to 100 chars, so adding a thread-title line requires truncating both parts
- (event) No conversation-level label/tag mechanism exists in Talk; thread labels must be built from scratch
- (decision) Working mode = Fast path at user request: infer remaining gaps, tag inferences [ASSUMPTION], present full draft for review instead of section-by-section coaching
- (decision) Document language = English per config document_output_language; conversation with user stays Vietnamese
- (event) Full PRD draft written: 38 FRs across 9 features, plus addendum.md carrying implementation detail, rejected alternatives and two unrelated upstream defects
- (decision) FR-29 will be built on the three dead columns talk_attendees.has_unread_threads/_thread_mentions/_thread_directs, which already exist with zero callers - no migration needed for conversation-level thread-unread indication
- (decision) Locked blocks six write paths, enumerated as separate testable consequences in FR-5: direct message, bot message, file share/rich object, poll, scheduled-message accept, scheduled-message fire (background job, no live request)
- (assumption) Nine open questions raised; three block downstream work: shared read-marker acceptability (Q1, blocks 4.7), state naming vs other tools (Q3, pending research), push size budget for thread title (Q9, blocks FR-30/FR-34)
- (decision) REVERSAL of earlier decision: per-thread read markers ARE in scope. User rejected the shared-marker model outright ('thread nao unread thread do'). Supersedes the earlier decision to derive unread from talk_attendees.last_read_message alone
- (decision) Per-thread unread requires that reading the main chat also not clear thread unread, otherwise the same defect returns inverted; conversation unread therefore splits into main-chat unread plus a has-unread-threads indication (the three dead columns)
- (decision) Storage cost of per-thread markers bounded by materialising a row only for threads a participant actually reads, falling back to the conversation marker when no row exists; row count tracks engagement, not participants x threads. This is the answer to the cost that likely drove upstream to drop the columns
- (decision) Open question 1 resolved by user: shared-marker limitation not acceptable. Accepted consequences are storage growth and permanent divergence from upstream on talk_thread_attendees
- (change) FR renumbering: section 4.7 grows to FR-26..FR-30, notifications become FR-31..FR-35, navigation becomes FR-36..FR-39; total 39 FRs
- (event) Comparable-product research returned (Discord, Slack, Teams, Zulip with cited sources). Key finding: 'Closed' carries three incompatible meanings across tools; 'Locked' is safe and consistent; 'Tag' is unusable as a UI word because Teams tags are at-mentionable people groups
- (event) Research validates three design choices: soft-close matches Zulip's deliberate non-blocking Resolve; the FR-3/FR-6 permission asymmetry matches Discord's documented rule (unarchive needs SEND_MESSAGES unless locked, then MANAGE_THREADS); storing state in a column avoids the link-breakage bugs Zulip hit by encoding state in the topic name
- (decision) Label bounds taken from Discord forum-tag precedent instead of guesses: 5 labels per thread, 20 distinct labels per conversation
- (decision) UI must say 'label', never 'tag' - Microsoft Teams tags are at-mentionable groups of people, so 'tag' misleads users arriving from Teams
- (event) Discarded premise: Microsoft Teams has no documented per-conversation lock/unlock. The real features are channel-scoped moderation (replyRestriction) and Viva Engage's Close conversation, a different product. Never entered the PRD
- (decision) Open question 3 resolved by user: keep Ongoing/Closed/Locked as the interface strings. Accepted risk that GitHub/Jira/Viva Engage users read 'Closed' as finished-and-shut; mitigated by a new FR-7 consequence requiring the Closed indicator to convey that posting reopens the Thread
- (decision) Open question 1 (conversation-marker fallback for never-opened threads) deferred to the architecture workflow at user direction rather than resolved here; PRD recommends the separate-baseline-column option
- (event) Memlog audit complete: all 30 entries accounted for. 24 captured in prd.md or addendum.md, 4 correctly superseded by later entries (shared-marker unread, 38-FR count, nine-open-questions count, first Q1 resolution), 2 process-only with no document home (Fast path mode, document language)
- (change) Audit correction: the permission model entry was logged as (assumption) but issue #22 item 3 states 'nhung nguoi mo thread hoac quan tri cua room' verbatim, which grounds author-OR-moderator in the source. PRD section 4.2 correctly states it as a decision with no ASSUMPTION tag; the log entry was the imprecise one
- (change) ERROR FOUND BY REVIEW, addendum 5.1 claim was false: ConversationTagService DOES ship in this build (lib/Service/ConversationTagService.php). It is per-USER not shared - createTag(userId,name), getTags(userId), deleteAllTagsByUserId, plus reorderTags and setCollapsed for sidebar sections - so it is not directly extensible for shared thread labels, but it is a real in-product precedent that the addendum denied existed
- (event) ConversationTagService constants land on the same numbers the PRD took from Discord: MAX_TAG_IDS_PER_CONVERSATION = 20, MAX_CUSTOM_TAGS_PER_USER = 100, MAX_TAG_NAME_LENGTH = 250. Also ships updateTag and deleteTag, the rename/delete surface FR-22 deferred to v2, and TagNameAlreadyInUseException for name uniqueness
- (change) Naming collisions the research missed by surveying competitors but not this build: Capabilities.php already declares conversation-tags, pinned-messages and archived-conversations-v2. So 'tag' is ALREADY a user-facing Talk word, which undercuts the never-say-tag rule; and 'Pinned Thread' collides with the shipped pinned-messages feature (PinnedMessage.vue, PinnedMessageItem.vue)
- (decision) C1 resolved by user: unreadMessages will EXCLUDE thread replies, and the unreadMention gate at RoomFormatter.php:335 is fixed alongside. This is a deliberate semantic change to a shipped API field - the single documented exception to the additive-only rule - and needs a new count-with-thread-filter override mirroring the existing CommentsManager read override
- (decision) Item 8 stays in MVP but is documented as groundwork: web deep-linking already works because setLink sits outside the push guard, so MVP ships no user-visible change for issue item 8; its value is enabling the mobile epic
- (decision) Vocabulary changed by user to match this build rather than competitors: Thread Label becomes Thread Tag (Talk already ships conversation-tags), Pinned Thread becomes Featured Thread (avoids collision with the shipped pinned-messages feature). Glossary-wide rename
- (decision) No migration backfill of thread state: user chose to accept that the Directory shows every pre-existing thread as Ongoing on day one, and not to pull bulk close into MVP. Recorded as a known limitation with SM-1 measuring whether it self-corrects
- (decision) Featured Threads appear in the Directory regardless of the state filter, with state shown. Resolves the pinned-plus-filter contradiction and makes a Closed Featured Thread with unread messages reachable so its badge is clearable
- (event) Peer session frontend-check verified 13 front-end claims: 0 wrong, 5 line-number drifts, 3 scope nuances - thread system-message constants live in THREE lists in src/utils/message.ts (50-51, 76-77, 90-91) not one; router.ts has a second factory createMemoryRouter at 96-102 also naming a conversation route; ThreadHeader.vue is filed under RightSidebar/Threads but renders in TopBar.vue:54 and ChatView.vue:27
- (event) Notification subject list confirmed at Notifier.php:275 - nine thread-capable subjects: reply, mention, mention_direct, mention_group, mention_team, mention_all, chat, reaction, reminder. FR for thread title in notifications must cover all nine, not just reply and mention
- (change) WRONG W1: setObject() APPENDS, it does not overwrite. The second call re-reads getObjectId() which already holds the first result, so the value is {token}/{messageId}/{threadId}. Proven by the repo's own test threads.feature:223 asserting object_id = room1/Message 2/Thread 1, and by the shipped Android client reading index [1] as messageId and [2] as threadId. The lost-message-id claim was false and the FR built on it must be reframed
- (change) WRONG W2: the guard at Notifier.php:633 wraps BOTH setObject calls, so a push payload's object id is the bare room token - it carries neither message id nor thread id. Worse than the PRD claimed, and it strengthens rather than weakens the push requirement
- (change) WRONG W3: the claim that mobile clients cannot route into a thread no matter what they implement is false. Android already routes on push by fetching the notification over OCS and reading the thread id from that object id (NotificationWorker.kt:484-493). The real failure is the narrower fallback at :496-513 when the OCS fetch fails
- (change) WRONG W4/W5/W6: the Locked write-path enumeration was three short. Missed: real file sharing via Chat/SystemMessage/Listener.php handle():120 -> :435 threadId -> :448-455 posts file_shared and :459 can CREATE a thread; ChatController::postAttachmentToRoom():2505 which reads threadId at :2574 and does NO validateThread at all; and in-process bot answers via BotService.php:305. Nine paths, not six
- (event) Constraint on the Locked guard design: all four rich-object, poll, file-share and attachment paths post via addSystemMessage(), so a ChatManager-level guard cannot use 'is a system message' as its exemption - that would exempt four of the nine paths. The exemption must be narrower, e.g. only state-change messages this PRD introduces
- (event) Confirmed sound: setLink() is not part of push payloads (DecryptedPushMessage.kt declares no link field), so the PRD's framing of the web-versus-push split holds. Code-claim review totals: 6 wrong, 18 drifted, ~137 confirmed, 5 unverifiable
- (change) Revision 2 written. prd.md and addendum.md both rewritten rather than patched, because the vocabulary rename plus FR renumbering plus the corrections touched too much for surgical edits. Revision 1 preserved as prd.superseded-r1.md and addendum.superseded-r1.md so corrections can be checked against what they replaced
- (decision) Triage outcome, fixed in r2: FR-26/FR-27 contradiction resolved by scoping FR-28 to threads with a marker and routing the never-opened case to open question 1; pinned-plus-filter contradiction resolved by FR-14 making Featured Threads survive the state filter; unclearable-badge resolved by FR-29 mark-one and mark-all reaching filtered-out threads; missing manager-reopen-Closed added as FR-6; concurrency added to FR-2, FR-4 and an NFR; metrics rewritten to be DB-measurable with thresholds and a required pre-work baseline; notification coverage widened to all nine subjects; lifecycle notifications added as FR-34; Files-sidebar router added as FR-42; the false overwrite requirement deleted
- (decision) Triage outcome, deferred with owner: open question 1 to architecture (requester's direction); questions 2, 3, 9, 10 to requester as non-blocking; 4 to Clavis; 5 to engineering; 6 to requester; 7 to engineering as blocking FR-33 and FR-37; 8 root-message deletion to PM with engineering. Ten questions, none silently dropped
- (event) Reviewer gate complete: four reviewers ran - rubric walker (strong overall, 1 critical, 6 high), adversarial (5 critical, 9 high), code-claims verifier (6 wrong, 18 drifted, ~137 confirmed), issue reconciliation (0 dropped, 2 reinterpreted, 5 partial). Reports kept in the workspace
- (change) Editorial pass on addendum.md applied (31 edits, 7015 to 6974 words, no sections moved or renumbered). It caught an ambiguous cross-reference the author missed: two mentions of 'section 7's additive-only rule' pointed at the PRD's section 7 while the addendum's own section 7 is Test surface. Both now say 'the PRD's section 7'
- (change) Editorial pass on prd.md applied (8 dedupe/condense edits, 5 long sentences split, 14930 to 14826 words, no FR/UJ/SM/section renumbering). Assumptions Index item 11 then split in two so the roundtrip is exactly 1:1 - 22 inline markers, 22 index entries
- (event) PRD finalized: status set to final, 42 FRs, 5 UJs, 11 SMs, 10 open questions, 22 assumptions. External handoffs list is empty in customize.toml so none were executed
- (event) r3 amendment driven by the bmad-spec open-question resolution session (see _bmad-output/specs/spec-clavis-spreed/.memlog.md). All ten open questions closed.
- (decision) PRD r3 changes: FR-43 added (optional free-text lock reason, v1, OQ-9); FR-5 reaction refusal confirmed (OQ-2); FR-35 identifier-is-routing confirmed (OQ-3); section 5 scale figures become a design target (OQ-6); section 10 Success Metrics REPLACED by Acceptance Criteria AC-1..AC-7 plus watch items AC-W1/AC-W2 because no reference deployment exists (OQ-4); section 13 replaced by a resolution table; section 16 items 6/19/22 struck and item 23 added (seeded fixture representativeness); bots excluded by decision not deferral (OQ-10); all SM-* references retargeted to AC-*.
