---
scope: Thread management and navigation in clavis-spreed (fork of Nextcloud Talk stable34 / Talk 24.0.3): lifecycle, authority, directory, search, featuring, tags, per-thread unread, thread-aware notifications, navigation entry points. Server (PHP) + web client (Vue). Excludes federation and mobile clients.
purpose: build-substrate
altitude: feature
updated: 2026-08-08T21:25
---

- (event) Architecture run bound; intent=Create; altitude=feature; driver=prd.md r2 + addendum.md r2 (42 FRs, 10 open questions) from prd-clavis-spreed-2026-08-08. Working mode = Fast path at user request; spine is the only deliverable.
- (constraint) Inherited silently from the PRD, not re-decided: scope excludes federation and mobile clients; Ongoing/Closed/Locked as interface strings; vocabulary Thread Tag and Featured Thread; free-text tags capped 5 per Thread and 20 per Conversation; no migration backfill; per-thread read markers are in scope over the shared-marker model.
- (version) Stack read from the repo, not inferred: appinfo/info.xml declares Talk 24.0.3 with nextcloud min-version=34 max-version=34; composer.json pins platform php 8.2; package.json pins vue 5.3.40-line (^3.5.40), pinia ^3.0.3, vue-router ^5.2.0, typescript ^5.9.3, vitest ^4.1.10, rspack ^2.1.7, @nextcloud/vue ^9.9.0.
- (event) Open question 5 checked on the web: no upstream announcement of thread lifecycle, close or lock work was found on nextcloud/spreed or the Nextcloud channels as of 2026-08-08. Treated as unannounced, low confidence, not proof of absence. Shaping toward upstream is therefore not possible; AD-17 contains the divergence instead.
- (decision) Paradigm named: layered service-oriented over the Nextcloud app framework (Controller to Service to Mapper/Entity) with an event-listener seam on the server and a Pinia-store-owned SPA on the client. The feature-specific overlay is that ThreadService is the aggregate boundary for all Thread state.
- (decision) AD-1 ThreadService is the sole write seam for Thread rows and the Thread cache. Binds all epics. Prevents each epic writing talk_threads through ThreadMapper with its own or missing cache invalidation, which is the top risk in PRD section 12. Rule includes the toJson/fromJson and SelectHelper obligations, because a field missing from either vanishes on a cache round trip or is absent from joined reads.
- (constraint) Cache shape verified in lib/Service/ThreadService.php: distributed cache, prefix thread/, key thread/{roomId}/{threadId}, TTL 900s, negative caching writes an empty string at :76 so a miss can make a newly visible Thread invisible. createThread :51 and renameThread :126 re-set the entry; updateLastMessageInfoAfterReply :266 removes it.
- (decision) AD-2 one enforcement seam for Locked, at ChatManager::sendMessage and ChatManager::addSystemMessage, with an exemption that is an explicit allow-list of the state-change verbs this feature introduces. Chosen over nine controller-level guards because path 9 is a background job with no request context and path 3 is an in-process bot answer that never touches a controller.
- (event) AD-2 choke point verified against the two paths that could have escaped it: lib/BackgroundJob/SendScheduledMessages.php calls chatManager->sendMessage at :161 and addSystemMessage at :180; lib/Service/BotService.php calls sendMessage at :305 and addSystemMessage at :324. Both funnel correctly, so the ChatManager seam does cover all nine paths.
- (constraint) AD-2 has a precondition the addendum found: ChatController::postAttachmentToRoom passes threadId straight into addSystemMessage (verified at :2601) with no validateThread call anywhere on that path. Validation must be added before any guard can be relied on there.
- (decision) AD-3 Thread Manager authority gets exactly one implementation, lifting the expression currently inline at ThreadController.php:224-228 into ThreadService::isThreadManager. Preserves the existing degradation that upstream already wrote at :218-222, where a root message that cannot be loaded leaves only moderators qualified.
- (decision) AD-4 refusals are typed: Thread Locked, insufficient authority and Thread not found carry distinct exception classes and distinct OCS error identifiers. Locked is not folded into the existing permission error string. FR-5 depends on this because a composer cannot explain a refusal it cannot identify.
- (decision) AD-5 resolves PRD open question 8. Thread lifetime is bound to the root comment ROW, and the two deletion kinds differ. Verified in ChatManager::deleteMessage :626-682: user deletion is a tombstone that sets VERB_MESSAGE_DELETED and keeps the row, so the Thread survives with its id, state, tags and featuring. Message expiry is different: deleteExpiredMessages :1321 delegates to the platform deleteCommentsExpiredAtObject, which hard-deletes comment rows and would orphan talk_threads. The expiry job must therefore reap orphaned Thread and thread-attendee rows, bounded per run.
- (decision) AD-6 resolves PRD open question 1 by adopting addendum option 3, the separate fallback baseline, over accept-the-defect and materialise-on-marker-advance. talk_thread_attendees reinstates last_read_message and last_mention_message; a row is written only when a participant reads that Thread; a Thread with no row resolves against a new thread-read baseline column on talk_attendees advanced only by mark-all-threads-read and by joining. read_privacy is not reinstated. Chosen because it is one column and no per-thread write burst, and because it composes with FR-29 which the PRD already requires.
- (decision) AD-7 a talk_thread_attendees row means has state, never is subscribed. The addendum found this correctness is currently incidental: findAttendeesForNotification and getRecentByActor happen to filter on notification_level, so a read-created NOTIFY_DEFAULT row does not leak into notifications or the Followed Thread List. Making it a rule plus a test turns an accident into a contract, because the unread epic creates the rows and the notification epic reads them.
- (decision) AD-8 conversation unread splits. unreadMessages counts non-thread messages only through a new counting override in Talk CommentsManager mirroring the existing read override at :101-133; the unreadMention and unreadMentionDirect gate on unreadMessages !== 0 at RoomFormatter.php:335-336 is removed; the lastReadMessage equals lastMessage short-circuit at :325-330 is removed because the last message may now be a thread reply; the unreadCountCache key gains a semantics token because the existing key {roomId}-{lastReadMessage} would otherwise be read across an upgrade with the new meaning.
- (constraint) AD-8 needs no migration for the conversation-level thread indication: talk_attendees.has_unread_threads, has_unread_thread_mentions and has_unread_thread_directs already exist end to end, verified in Attendee.php:150-152 and :184-186, AttendeeMapper.php:315-317 and SelectHelper.php:112-114, with zero callers. Writing the callers is the whole of that storage work.
- (decision) AD-9 no per-Thread query per rendered row. Unread for a page of Threads resolves in a fixed number of queries per page regardless of page size, and counting stops at the FR-32 display cap. A response-time baseline of the current nested list is captured before the first Directory change ships, otherwise SM-C3 cannot fail.
- (decision) AD-10 Thread Tag identity is a Conversation-scoped row, not a string on the Thread, because FR-24 binds colour to the tag text within the Conversation. Two new tables: a tag row unique on room plus normalised name, and a Thread-to-tag association. ConversationTagService is copied rather than extended, since every one of its methods is keyed by userId and Thread Tags are shared.
- (decision) AD-11 text matching is normalised at write time in one shared helper, producing stored normalised columns for both talk_threads.name and tag names. Prevents FR-18 diacritic-insensitivity from depending on the deployment database collation, which is not one engine across customers, and prevents titles and tags growing two different fold implementations.
- (decision) AD-12 API discipline: the existing threads/recent endpoint gains optional state, tag and pagination parameters with behaviour-preserving defaults rather than a parallel list endpoint; new endpoints only for operations that do not exist; the composed notification object id may gain a trailing position but never a reorder; two new capability flags, one for thread management and one for the AD-8 unread semantics, and the unconditional shipped threads flag is not reused; every mutation responds with the full updated Thread representation; the generated openapi json and ts are regenerated in the same change.
- (decision) AD-13 concurrency is last write wins with the response as the correction: no optimistic-concurrency token, mutations apply unconditionally, the client replaces its local copy with the response rather than with what it sent, and other participants converge through the relayed state-change system message.
- (decision) AD-14 every Thread state change emits a system message registered in all five places in the same change: src/constants.ts, the three lists in src/utils/message.ts, SYSTEM_MESSAGE_TYPE_RELAY in lib/Signaling/Listener.php, and the parser in lib/Chat/Parser/SystemMessage.php. That same constant set, and nothing else, is the AD-2 exemption list, which ties the guard exemption to the state machine so the two cannot drift apart.
- (decision) AD-15 client Thread state lives in useChatExtrasStore; messagesService is called only from that store; components read getters and dispatch actions. Prevents the Directory, the thread header and the left-sidebar Followed Thread List each holding a copy that diverges after a relayed system message.
- (decision) AD-16 thread and Directory addresses stay query parameters on the conversation route with no new named route, and must resolve under both router factories. Exists because createMemoryRouter at src/router/router.ts:96-102 serves the Files sidebar and declares its own conversation route; a named route added only to createTalkRouter would 404 there. This is FR-42 promoted to an invariant.
- (decision) AD-17 divergence is contained in new files: new behaviour lands in new classes and migrations wherever a seam allows, edits inside upstream-owned methods are kept to the smallest diff and carry a comment naming the requirement, and the migration reinstating the dropped columns states why they are back so the next merger does not read them as an accident.
- (constraint) AD-17 has a hard prerequisite the addendum recorded: the clone is shallow at depth 1, so rebasing onto upstream releases is impossible until git fetch --unshallow is run. That must precede the first commit of this work.
- (decision) AD-18 federation degrades by omission: no new Thread field is proxied and no new Thread endpoint is exposed through lib/Federation/Proxy/, and the client hides state, featuring, tag and per-Thread-unread affordances on a federated conversation rather than showing inert ones.
- (decision) AD-19 migrations are additive, defaulted and re-runnable, with no data backfill, so every pre-existing Thread reads as Ongoing, unfeatured and untagged on day one per PRD section 9.3. The one initialisation that does happen is the AD-6 thread-read baseline, seeded from each participant existing conversation read marker so nobody wakes to a room full of badges.
- (assumption) Featured Threads are modelled as a boolean on talk_threads with featured-first ordering by last_activity, not a manual ordering value. Manual ordering among Featured Threads is deferred. Correct this if the requester expects to arrange the featured block by hand.
- (assumption) Thread State is stored as an integer column with named constants, matching how notification_level is already handled on talk_thread_attendees, leaving room for a fourth state without another migration.
- (assumption) last_mention_direct is reinstated only if the three-way unread split in FR-31 is carried end to end to the client. If the client shows a single mention indication, the column is not restored and the third dead talk_attendees column stays uncalled.
- (question) Open question 7 is not resolvable in this spine: the push payload size budget must be measured against the platform notifications app encryption limit, not inferred from the 100-character preview truncation at Notifier.php:686-689. Deferred with the condition that FR-33 and FR-37 cannot be committed until it is measured. Owner engineering.
- (question) Open question 2, whether reactions count as writes in a Locked Thread, stays a product call. AD-2 makes it a one-line change to the guard either way, so it does not block the epics.
- (event) Unrelated upstream defect that touches AD-14 and is worth carrying into the epics: BotService.php calls createThread with the invoking comment id at :321 while the thread_created system message at :328 uses the bot own reply id, so the thread row and the system message disagree about the root. Any AD-14 registry work in that file will meet it.
- (event) Memlog seeded; distilling ARCHITECTURE-SPINE.md next.
- (decision) AD-20 added during input reconciliation, which caught a quiet requirement the AD structure had dropped: PRD section 6.2 Data and Privacy was absent from the spine entirely. Thread Titles and Tags carry message-grade sensitivity; notifications reuse the shipped sensitive-conversation setting to withhold titles wherever message previews are already withheld; no Thread Title may enter any part of a push payload outside the envelope the customer server encrypts for the device, because the proxy being unable to read what it relays is a customer commitment. Open question 3 added to Deferred as the residue, since AD-20 settles titles but not identifiers.
- (event) Reviewer gate dispatched with user approval: lint_spine.py clean after two false-positive rewordings, then three parallel reviewers (rubric walker, reality-and-version check, adversarial seam attack) against ARCHITECTURE-SPINE.md.
- (event) Peer research digests from the PM run arrived mid-gate (slack-research, teams-research, frontend-check). Checked against the spine: the frontend-check nuances that matter were already load-bearing in it (three lists in src/utils/message.ts behind AD-14; the second createMemoryRouter factory behind AD-16; the chatExtras store shape behind AD-15), so nothing needed correcting. teams-research confirms the Teams lock premise the PRD had already discarded. Digest relayed to the reality-check reviewer so it spends its budget on the unverified server-side half.
- (constraint) Comparable-product evidence recorded against PRD open question 2 rather than left as a bare assumption: Slack per-thread freeze pauses replies AND reactions together; Viva Engage Close conversation deliberately keeps reactions alive. The split is real, so FR-5 blocking reactions is an argued position, not a settled one. Deferred entry updated to say so.
- (constraint) Premise correction from slack-research, noted but not acted on: Slack DOES have a per-thread freeze, admin-gated on Enterprise and reversible, so the comparables section overstated the negative. It corroborates the Locked design rather than challenging it, and the PRD is final at r2, so no spine change follows.
- (constraint) Contrast worth carrying forward on AD-8: Slack exposes thread-reply inclusion in the badge count as a per-user preference, whereas AD-8 redefines the field server-side behind a capability flag. The requester already resolved this direction and PRD section 7 records it as the single deliberate exception, so AD-8 stands; the contrast is logged in case badge complaints (SM-C5) later argue for a preference instead.
- (event) Reality-and-version reviewer returned: conditional pass, 30 confirmed / 6 drifted / 5 WRONG, three of the wrong ones load-bearing premises under ADs. All five independently re-verified against source before applying; all five held. The spine was wrong, not the reviewer.
- (change) AD-7 rewritten on a false premise. The claim that findAttendeesForNotification and getRecentByActor both filter identically was wrong: ThreadAttendeeMapper::findAttendeesForNotification excludes NOTIFY_DEFAULT (0) while ThreadService::getRecentByActor excludes only NOTIFY_NEVER (3), so a read-created row at NOTIFY_DEFAULT IS included in the Followed Thread List, and threads.feature:224 already asserts a notificationLevel 0 thread under subscribed threads. AD-6 lazy rows would have enrolled every opened Thread into every participant followed list. New rule: subscription gets its own explicit column, set by ensureIsThreadAttendee on reply and not by a read-created row, with getRecentByActor gaining the predicate in the same change.
- (change) AD-14 was missing the registry that actually drops a message. Signaling Listener notifySystemMessageSent returns early when the emitter passed skip-last-activity-update unless the verb is in a hard-coded inline array separate from SYSTEM_MESSAGE_TYPE_RELAY; thread_renamed relays only because it is named there and ThreadController::renameThread does pass skip. Registry count corrected from five to six and the inline array named explicitly.
- (change) AD-10 contradicted AD-11 and was fixed. ConversationTagService::normalizeTagName is private and does trim plus length check only, no case-fold and no diacritic-fold, so copying it would produce exactly the variant-duplicate defect AD-10 forbids. Identity normalisation now comes from AD-11 shared helper; only the exception vocabulary and the caps are copied.
- (change) AD-2 overclaimed and was narrowed. ChatManager is the choke point for content POSTED into a Thread, eight of nine paths, not the only write path to comments: ReactionManager writes via commentsManager->save directly, as do editMessage, deleteMessage, pinMessage and unpinMessage; and scheduleMessage writes no comment at all, so it is validated at request time and again when it fires. Diagram redrawn to show both carve-outs.
- (change) Deferred entry for open question 2 corrected. The PRD called blocking reactions in a Locked Thread a one-line change; it is not, because reactions bypass both choke points, so answering yes means building a third enforcement seam that also covers edits, deletes and pins. Now sized as its own story rather than a toggle.
- (change) AD-19 contradicted itself and was resolved by design change rather than by wording. It said no data backfill and then seeded the AD-6 baseline from every attendee read marker, which is the table-wide UPDATE its own Prevents clause forbids. New design: the baseline column ships NULL and is frozen lazily, capturing the pre-advance conversation marker the first time that marker would move, on a row write that already happens. Zero backfill, identical semantics, and the day-one no-badge-flood property now comes from NULL falling back to the conversation marker.
- (version) Stack Database row corrected: CI tests FOUR engines on every PR, not three - phpunit-oci.yml and integration-oci.yml add Oracle alongside MySQL/MariaDB, PostgreSQL and SQLite. Oracle is the strictest and binds AD-11 normalisation, AD-9 grouped queries and AD-19 migrations.
- (constraint) Watch item added to AD-17 from the reviewer web check: an open upstream issue reports Threads persisting after all their messages are deleted, the same ground AD-5 reaper occupies. If upstream lands its own fix it is a merge collision to reconcile deliberately. Also confirmed: still no upstream close/lock/lifecycle work announced, so AD-17 core assumption holds.
- (event) Confirmed safe to build on, per the reviewer line-by-line pass: the AD-1 six-place field checklist, the cache shape (distributed, thread/{roomId}/{threadId}, 900s, negative caching), AD-3 authority expression and its root-missing degradation, AD-5 tombstone-versus-expiry distinction and that nothing reaps talk_threads today, write-path 7 missing validateThread, AD-16 second router factory, and the shallow clone.
- (change) AD-16 widened after verification: createMemoryRouter is not just the Files sidebar, it backs FOUR entry points - mainFilesSidebar.js:23, mainPublicShareSidebar.js:69, mainPublicShareAuthSidebar.js:42 and mainFloatingCall.ts:43. The acceptance condition now names all four, since public-share and floating-call are the surfaces nobody remembers to test. Also cited the shipped precedent: SearchMessagesTab.vue navigates with name conversation plus query threadId, the one route name both factories declare, which is exactly the pattern AD-16 mandates.
- (change) Source-tree seed corrected to carry a warning: ThreadHeader.vue is filed under RightSidebar/Threads/ but renders in TopBar.vue and ChatView.vue, never in the sidebar. Without the warning the tree points implementers at the wrong component for AD-15 thread-header work.
- (event) Reviewer reported AD-2 findings F4 and F7 as only half-applied; re-checked the file and they were fully applied - the reviewer had read a copy from mid-edit. No action needed, recorded so the discrepancy is not re-investigated later.
- (event) Rubric walker and adversarial reviewer both went idle without returning summaries; both had written their review files (review-rubric-walker.md 334 lines, review-adversarial-seams.md 853 lines, 15 constructed collisions AS-1..AS-15). Findings read off disk directly rather than lost. Adversarial verdict: not yet safe to hand to five epics; rubric verdict: three blockers, two of them artefacts of the previous round of fixes.
- (change) H1 applied, and it was the one rule in the spine that made working code worse. AD-1 mandated re-setting the cache entry from the mutator entity; upstream updateLastMessageInfoAfterReply correctly REMOVES it. Under AD-13 last-write-wins, re-setting republishes a value the loser already superseded and pins it for the full 900s TTL. New rule: mutators invalidate only, findByThreadId is the sole writer of the cache entry. Conventions row updated to match.
- (change) AS-1 applied: AD-1 and AD-10 named two different owners for the tag tables - AD-1 claimed ThreadService writes the new tag mappers while AD-10 created ThreadTagService. Direct contradiction with no third reading. Now stated as two aggregates: ThreadService owns thread and thread-attendee rows, ThreadTagService owns the tag tables, neither writes the other.
- (change) AS-2 applied: AD-6 said mark-all-threads-read clears existing rows, but AD-7 had just moved subscription onto those same rows, so clearing a badge would silently unfollow the Thread. Rule and diagram both changed to reset the read columns and never delete the row.
- (change) AS-3 applied and it was worse than the previous fix recorded. notifySystemMessageSent contains FOUR verb-dependent checks, not two: SYSTEM_MESSAGE_TYPE_RELAY, the inline early-return array, the $thread lookup branch, and the threadInfo payload branch. The first two decide whether the message relays at all; the last two decide whether it arrives carrying its Thread. The earlier amendment fixed only the first two, so a new lifecycle verb would relay and arrive with no thread context - a failure that reads as a rendering bug rather than a missing registration.
- (change) AS-7 applied: AD-5 reaper against AD-1 warm cache. A reaped Thread whose cache entry is still positive makes validateThread answer true for up to 900s, so content is accepted into a Thread that no longer exists. The reaper must invalidate each reaped entry in the same pass.
- (change) AS-5 applied: AD-12 now states there is exactly ONE Thread representation, same builder for mutation responses and list rows. Two shapes is the failure AD-13 turns into a crash, because the client replaces its store copy with whatever the mutation returned.
- (change) AS-6 applied: ordering is now part of the contract. Paginated results carry a total order - featured first, then last_activity descending, then thread id descending as tie-break - so page 2 cannot repeat or skip a row when last_activity ties or moves. Client renders the server order and never re-sorts.
- (change) AS-12 applied: caps are enforced server-side at write time from one constant each - unread display cap, five tags per Thread, twenty tags per Conversation, featured cap. A sort cannot enforce a count and a disabled button is not enforcement.
- (change) AS-15 applied: AD-15 now says a relayed message for a Thread the store never loaded is normal - the store either adopts the Thread the payload carries or ignores the message, and never creates a half-populated entry that later reads as loaded.
- (change) Rubric M1 applied: the AD-2 correction had bundled FR-5 edit and delete refusal into the deferral of open question 2, so a no on reactions would have silently dropped a requirement the PRD had decided. Deferred entry now scoped to reactions only, with edits and deletes stated as decided.
- (decision) Rubric H3 resolved rather than deferred, because the colour part is a schema decision not a styling one: Thread Tag colour is a bounded palette stored as an index, not a free-form hex. Free hex cannot meet contrast in both light and dark themes, cannot follow customer Nextcloud theming, and makes every new theme a data migration. Accessibility and theming added as a conventions row - design tokens, never colour alone for state, keyboard-navigable Directory.
- (event) Adversarial reviewer returned its summary against spine r2, before the previous round of fixes landed, so AS-1, AS-2, AS-5, AS-7 and the AD-14 gate count were already closed when it reported. Three of its items were genuinely new and all three verified against source before applying.
- (change) AS-10 applied and it was the most severe finding of the whole gate. The lazy freeze in AD-6 named no seam. ParticipantService::updateLastReadMessage is the single choke point with six callers, and two of them are POSTS not reads - ChatManager:474 advances the conversation marker on every message sent, including a thread reply. A freeze wired to reading the main chat would leave a participant whose first action is a thread reply with their marker past everything and a NULL baseline, resolving every never-opened Thread as read - permanently zero badges, the exact defect AD-6 exists to prevent. Freeze now fires inside updateLastReadMessage on ANY advance, with one exemption: the UNREAD_MIGRATION repair at RoomFormatter:323, which reconstructs a legacy marker rather than reflecting a read.
- (change) AD-8 gained the corollary AS-10 exposed: posting a thread reply must stop advancing the conversation read marker at all. It does today, and once the count excludes thread replies that advance silently marks unread MAIN-CHAT messages read as well - the split leaking backwards. A thread reply advances that Thread own marker instead.
- (change) AS-17 applied: the new subscription column on talk_thread_attendees must ship NOT NULL DEFAULT true. Every pre-existing row was created by a reply or a notification-level change, so all of them are genuine subscriptions; DEFAULT false would unsubscribe every existing follower at upgrade and fail threads.feature:224, and AD-19 forbids the repair UPDATE. The column default has to carry it.
- (change) AS-16 applied: AD-2 named a third seam for reactions, edits, deletes and pins and assigned it to nobody while FR-5 still refuses every write. Seam is now owned by the lifecycle epic and stated as not optional; only the REACTION case is deferred, edits deletes and pins are in scope and ride it. Without this a Locked Thread stays editable and deletable while reporting itself locked.
- (change) AS-3 hardened beyond the previous fix: rather than a checklist of four edit sites inside one function, the four verb checks in lib/Signaling/Listener.php are extracted into named constants declared together, each site testing membership. A four-site checklist is a rule that will be missed.
- (change) AS-5 sharpened: the single Thread representation is named as TalkThreadInfo, the wrapper the list endpoints already return, never the bare TalkThread - the client store indexes the nested id, so the bare entity throws or scrambles ordering.
- (change) AS-6 sharpened: pages are addressed by a keyset cursor on the (featured, last_activity, id) tuple rather than an offset, because last_activity moves while a participant pages and offset paging repeats one row while silently dropping another - and the id-keyed store absorbs the duplicate so only the loss survives. The existing client-side sort in the store is removed as part of this work rather than left to fight the server; changing a filter discards the cursor.
- (change) AS-12 sharpened: caps are published in the capability payload rather than restated client-side, and the unread display cap counts to cap+1 rather than to the cap - a client receiving exactly the cap cannot distinguish a full page from an overflowing one and could never render the 99+ form the cap exists to allow.
- (event) Reviewer conceded its own AD-7 proposal was wrong and the spine rule is better: it had proposed filtering getRecentByActor on != NOTIFY_DEFAULT, but threads.feature:224 asserts a REPLY-created row at notificationLevel 0 must appear in subscribed threads. Origin, not level, is the discriminator, which is why an explicit column is the only rule that works.
- (change) AS-13 applied: AD-1 field checklist spanned two incompatible row-key conventions - createFromRow reads a bare/t_ style row while the aliased SelectHelper branch emits th_*, each serving a different query. A field added correctly to every listed site still fails to load on one path. Normalising the two onto one prefix is now a preparatory commit that lands before the first new field, because a checklist spanning two conventions cannot be verified by reading the diff.
- (change) AS-8 applied: AD-2 never said what the guard reads. Both choke points now resolve the effective thread id through one shared helper and evaluate the guard BEFORE commentsManager->save - a refusal after the save is not a refusal. The sendMessage asymmetry closes with it: the explicit-threadId branch gains the validateThread call the reply branch already performs. FR-5 acceptance is one scenario per write path, not one for the family.
- (change) Rubric M3 applied: AD-3 fixed a signature that cannot reach the root comment it needs. ChatManager already injects ThreadService at ChatManager.php:121, so the reverse injection is a container cycle and the dependency diagram draws no such edge. The lookup now takes the low-level CommentsManager with the object-type/object-id check in one private helper beside isThreadManager, so two epics cannot answer it differently inside the one method AD-3 exists to keep singular.
- (change) Rubric M2 applied: FR-25 was orphaned - in MVP scope per PRD 9.1 but pushed to v2 by both AD-10 and Deferred, with no endpoint, authority check or propagation. Resolved by making tag recolour the one updateTag-shaped operation that IS v1, since AD-10 binds colour to the tag row and there is no other way to recolour. Rename, merge and delete stay deferred.
- (decision) AD-14 relay set redefined as a SUPERSET of AD-2 exemption list rather than the same list, which was the structural blocker under M2. A tag recolour must reach participants live without thereby becoming a write a Locked Thread permits; the lifecycle-transition subset alone is the exemption.
- (change) Rubric M4 applied and it made AD-18 implementable. Capabilities.php publishes three arrays and features-local (LOCAL_FEATURES) is the only channel telling a client a feature does not work over federation. The nearest precedent, threads, is FEATURES-only, so an epic copying it would advertise thread management as working on federated Conversations while AD-18 requires the client to hide it. Both new flags now go in FEATURES and LOCAL_FEATURES, and AD-18 names features-local as its mechanism.
- (change) AS-9 applied: the composed notification object id is now stated as variable-arity and absence-significant - a position is present with a real value or absent entirely, never padded, never a sentinel, and 0 is never valid. A new position may only follow the thread position and only on notifications already carrying it, so no position meaning depends on which earlier ones are present.
- (change) AS-14 applied: AD-16 acceptance was satisfiable by a router that renders nothing, because the two factories mount different components for the same route name. Resolves now means renders: every address declares Rendered (mounted-component test across all four memory-router entry points) or Degraded by design (parameter dropped, lands on main chat, matching AD-18). An address with neither declaration does not ship.
- (change) Rubric L1 applied: the AD-8 cache semantics token is APPENDED. Six sites invalidate the unread-count cache by room-id prefix, so prepending would silently disable all six and leave counts wrong for the full 1800s TTL. The prefix is part of the key contract.
- (change) Rubric L3 applied: FR-20 title search over the Followed Thread List happens inside that list own hand-built query, not the client store, matching the discipline AD-12 applies to the Directory. Two list surfaces filtering at two layers is the same divergence twice.
- (change) Rubric L4 applied: the conventions row now pins every bound as one server-side constant identical for every Conversation with a refusal that names the limit - unread display cap, tags per Thread, tags per Conversation, and the Featured-per-Conversation bound that previously had no home.
- (change) Rubric L2 applied: PRD open questions 9 and 5 are now visible in Deferred rather than invisible to an epic reader - a free-text lock reason recorded as weighed and cheap to add later, and the upstream-shaping question recorded as checked with no announced upstream lifecycle work.
- (event) Reviewer gate closed. Three reviewers ran plus a deterministic lint. Totals across the gate: 5 wrong claims and 6 drifts from the reality lens, 17 constructed collisions from the adversarial lens, 3 blockers and 11 tiered findings from the rubric walker. Every critical and high applied, then every medium and low. Two findings were mine to own rather than the reviewers: AD-1 cache rule made working code worse, and the AD-6 lazy freeze introduced AS-10 as an artefact of fixing AD-19.
- (event) spine finalized
- (event) r3 amendment driven by the bmad-spec open-question resolution session (see _bmad-output/specs/spec-clavis-spreed/.memlog.md). All ten open questions closed.
- (decision) Spine r3 changes: AD-2 third seam now covers reactions as decided scope; AD-6 last_mention_direct does not return (two-way client split); AD-14 locking constant carries FR-43 reason as message parameter data plus a nullable talk_threads column for present state; AD-20 extended to the lock reason and records identifiers as non-disclosive; schema delta updated on three rows; Deferred section retires four entries and downgrades the push-size-budget gate to story-level acceptance criteria; Operational envelope rewritten — no reference deployment, seeded fixture is an engineering deliverable.
